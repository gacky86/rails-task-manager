<h1>Edit a task</h1>
<%= render "form", task: @task %>

<%# you can avoid duplicate in new.html.erb and edit.html.erb using rendering _form.html.erb %>

<!--- <%= form_with model: @task do |f|%>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to list", tasks_path %> --->
